# Production API origin. In local development, leave this unset so the Vite
# proxy (see vite.config.ts) handles /v1 requests same-origin and the session
# cookie is preserved. Setting this to the real API in dev will send
# cross-origin requests and the backend cookie/session will not work.
# VITE_API_URL=https://api.atanda.ai/v1

# Required header on every API request.
VITE_APP_KEY=hjdbcsjhdshjbfdhsihbeiyreirdssdfdf1f26914ba
